import React from 'react';
import { Box, Typography } from '@mui/material';

const LeftFutter = () => {
    return (
        <Typography align='center' sx={{ opacity:0.5, fontSize: '0.9rem'}} >
            Explore the best of Seoul with NOVATRIP <br/>
             Discover hidden gems, local spots, and travel tips!
        </Typography>
    );
};

export default LeftFutter;